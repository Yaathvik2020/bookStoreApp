apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "bookstor-helm.fullname" . }}-pvc
  labels:
  {{- include "bookstor-helm.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.pvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.pvc.storageClass | quote }}
