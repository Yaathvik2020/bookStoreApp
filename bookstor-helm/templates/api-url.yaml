apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bookstor-helm.fullname" . }}-api-url
  labels:
  {{- include "bookstor-helm.labels" . | nindent 4 }}
data:
  VITE_API_URL: {{ .Values.apiUrl.viteApiUrl | quote }}
